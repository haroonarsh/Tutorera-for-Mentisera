import type { Metadata } from "next";
export const metadata: Metadata = { title: "First Session Guarantee", description: "Understand the eligibility, claim process, and limitations of TUTORERA's first-session guarantee.", alternates: { canonical: "/first-session-guarantee" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
