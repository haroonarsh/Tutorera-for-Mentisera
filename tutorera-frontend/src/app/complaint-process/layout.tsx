import type { Metadata } from "next";
export const metadata: Metadata = { title: "Complaint Process", description: "How to submit and follow up on a tutoring, safety, booking, or payment complaint with TUTORERA.", alternates: { canonical: "/complaint-process" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
