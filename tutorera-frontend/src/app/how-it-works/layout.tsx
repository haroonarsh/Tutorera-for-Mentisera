import type { Metadata } from "next";
export const metadata: Metadata = { title: "How TUTORERA Works", description: "Learn how students find verified tutors, compare profiles, book lessons, pay securely, and review completed sessions.", alternates: { canonical: "/how-it-works" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
