import type { Metadata } from "next";
export const metadata: Metadata = { title: "Become a Tutor | Teach Online Worldwide", description: "Create a tutor profile, submit qualifications for verification, and respond to relevant student requirements through TUTORERA.", alternates: { canonical: "/become-a-tutor" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
