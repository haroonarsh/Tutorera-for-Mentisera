import type { Metadata } from "next";
export const metadata: Metadata = { title: "Become a Tutor in Pakistan", description: "Create a tutor profile, submit qualifications for verification, and connect with students through TUTORERA.", alternates: { canonical: "/become-a-tutor" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
