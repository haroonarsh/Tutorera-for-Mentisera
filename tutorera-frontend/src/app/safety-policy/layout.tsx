import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tutoring Safety Policy", description: "Read TUTORERA's tutor verification, contact privacy, in-person lesson, reporting, and account safety standards.", alternates: { canonical: "/safety-policy" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
