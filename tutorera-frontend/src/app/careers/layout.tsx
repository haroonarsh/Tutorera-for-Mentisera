import type { Metadata } from "next";
export const metadata: Metadata = { title: "Careers at TUTORERA", description: "Explore opportunities to help build safer and more transparent education technology for Pakistan.", alternates: { canonical: "/careers" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
