import type { Metadata } from "next";
export const metadata: Metadata = { title: "TUTORERA Guide for Tutors", description: "Learn how tutor onboarding, verification, profiles, requests, bookings, fees, and payouts work on TUTORERA.", alternates: { canonical: "/help/for-tutors" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
