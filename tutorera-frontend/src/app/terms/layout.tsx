import type { Metadata } from "next";
export const metadata: Metadata = { title: "Terms and Conditions", description: "Terms governing student, parent, and tutor use of the TUTORERA marketplace.", alternates: { canonical: "/terms" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
