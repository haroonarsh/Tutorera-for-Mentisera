import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tutoring Pricing and Platform Fees", description: "Understand TUTORERA tutor rates, booking charges, the platform fee, and applicable GST before booking.", alternates: { canonical: "/pricing" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
