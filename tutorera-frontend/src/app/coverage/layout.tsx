import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tutoring Coverage | Global Learning, Local Choice", description: "Explore TUTORERA's online coverage worldwide and local home-tuition availability in enabled markets.", alternates: { canonical: "/coverage" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
