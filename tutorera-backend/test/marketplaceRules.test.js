const test=require("node:test");const assert=require("node:assert/strict");const{calculateMatchScore,sortMarketplaceOffers,TERMINAL_OFFER_STATUSES}=require("../dist/utils/marketplaceRules");const{calculateMarketplaceFees}=require("../dist/config/constants");
test("documented match weights total 100",()=>{const result=calculateMatchScore({subject:true,academicLevel:true,availability:true,location:true,teachingMode:true,withinPreferredBudget:true,rating:5,experience:10,responseRate:100});assert.equal(result.score,100);assert.equal(Object.values(result.breakdown).reduce((a,b)=>a+b,0),100)});
test("missing evidence earns no points",()=>assert.equal(calculateMatchScore({subject:false,academicLevel:false,availability:false,location:false,teachingMode:false,withinPreferredBudget:false}).score,0));
test("all six sorts are deterministic",()=>{const rows=[{amount:2500,matchScore:90,responseSeconds:60,completedSessions:5,profile:{averageRating:5,experience:8}},{amount:2000,matchScore:80,responseSeconds:30,completedSessions:10,profile:{averageRating:4,experience:4}}];assert.equal(sortMarketplaceOffers(rows,"best_match")[0].matchScore,90);assert.equal(sortMarketplaceOffers(rows,"lowest_rate")[0].amount,2000);assert.equal(sortMarketplaceOffers(rows,"highest_rated")[0].profile.averageRating,5);assert.equal(sortMarketplaceOffers(rows,"most_experienced")[0].profile.experience,8);assert.equal(sortMarketplaceOffers(rows,"fastest_response")[0].responseSeconds,30);assert.equal(sortMarketplaceOffers(rows,"most_sessions")[0].completedSessions,10)});
test("fees snapshot the approved model",()=>assert.deepEqual(calculateMarketplaceFees(2000),{subtotal:2000,studentFee:0,tutorFee:400,tax:60,studentTotal:2000,tutorNet:1540,feeConfig:{studentPlatformFeePercent:0,tutorPlatformFeePercent:20,taxRatePercent:15,minimumFee:0,currency:"PKR",effectiveFrom:"2026-08-30"}}));
test("terminal offer states cover every immutable result",()=>assert.deepEqual(TERMINAL_OFFER_STATUSES,["accepted","rejected","withdrawn","expired","not_selected"]));
