// backend/src/config/location.ts
// Shim — re-exports everything from the canonical geo/location module.
// Keeps backward compatibility for any imports using the old path.
export * from "./geo/location";
