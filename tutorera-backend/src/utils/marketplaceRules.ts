export const ACTIVE_OFFER_STATUSES = ["pending", "submitted", "viewed", "countered"] as const;
export const TERMINAL_OFFER_STATUSES = ["accepted", "rejected", "withdrawn", "expired", "not_selected"] as const;
export type MatchBreakdown = { subject:number; academicLevel:number; availability:number; location:number; teachingMode:number; budget:number; rating:number; experience:number; responseRate:number };
export function calculateMatchScore(input:{subject:boolean;academicLevel:boolean;availability:boolean;location:boolean;teachingMode:boolean;withinPreferredBudget:boolean;withinMaximumBudget?:boolean;rating?:number;experience?:number;responseRate?:number}) {
  const breakdown:MatchBreakdown={subject:input.subject?25:0,academicLevel:input.academicLevel?15:0,availability:input.availability?15:0,location:input.location?10:0,teachingMode:input.teachingMode?10:0,budget:input.withinPreferredBudget?10:input.withinMaximumBudget?5:0,rating:input.rating?Math.min(5,Math.round(input.rating)):0,experience:input.experience?Math.min(5,Math.round(input.experience/2)):0,responseRate:input.responseRate?Math.min(5,Math.ceil(input.responseRate/20)):0};
  return { breakdown, score:Object.values(breakdown).reduce((sum,value)=>sum+value,0) };
}
export function sortMarketplaceOffers<T extends {amount:number;matchScore:number;responseSeconds:number;completedSessions:number;profile?:{averageRating?:number;experience?:number}|null}>(offers:T[],sort:string){const comparators:Record<string,(a:T,b:T)=>number>={best_match:(a,b)=>b.matchScore-a.matchScore,lowest_rate:(a,b)=>a.amount-b.amount,highest_rated:(a,b)=>(b.profile?.averageRating||0)-(a.profile?.averageRating||0),most_experienced:(a,b)=>(b.profile?.experience||0)-(a.profile?.experience||0),fastest_response:(a,b)=>a.responseSeconds-b.responseSeconds,most_sessions:(a,b)=>b.completedSessions-a.completedSessions};return [...offers].sort(comparators[sort]||comparators.best_match)}
